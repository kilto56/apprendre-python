import React from 'react';
import ReactDOM from 'react-dom/client';

const App = (props) => {
  return (
    <div></div>
  );
};

let root = document.querySelector('#root');

ReactDOM.createRoot( 
  root
).render(<App />);